[package]
name = "aura-protocol"
version = "0.1.0"
edition = "2021"

[features]
default = ["rand"]
rand = ["dep:rand"]

[dependencies]
prost = "0.14"
bytes = "1.0"
thiserror = "2.0"
rand = { version = "0.9", optional = true }

[build-dependencies]
prost-build = "0.14"
